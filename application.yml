spring:
  flyway:
    enabled: false
  jpa:
    generate-ddl: false
    show-sql: false
    hibernate:
      ddl-auto: validate

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/pubSub
    username: root
    password: 123

server:
  servlet:
    contextPath: /api/v1
